//! Chrome/Chromium Local Storage and Session Storage decoder built on
//! [`leveldb_core`].
//!
//! Scaffold placeholder — the decoder lands on the `feat/leveldb-reader` branch
//! under strict TDD.
#![forbid(unsafe_code)]
#![cfg_attr(test, allow(clippy::unwrap_used, clippy::expect_used))]
