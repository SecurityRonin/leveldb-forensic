//! Pure-Rust, read-only, panic-free LevelDB record reader for forensics.
//!
//! Scaffold placeholder — the reader lands on the `feat/leveldb-reader` branch
//! under strict TDD (RED failing tests, then GREEN implementation).
#![forbid(unsafe_code)]
#![cfg_attr(test, allow(clippy::unwrap_used, clippy::expect_used))]
